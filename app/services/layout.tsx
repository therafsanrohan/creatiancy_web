import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Services & Capabilities",
  description: "We solve fundamental business problems through precision-engineered digital services. Explore our branding, content, and application solutions.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
